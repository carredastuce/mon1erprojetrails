<h1>Articles du blog</h1>

<p>
  <a href="<%= new_post_path %>" class="btn btn-success">Créer l'article</a>
</p>
<%= @posts.each do |post| %>
    <h2><%= post.title %></h2>
    <p> <%= post.content %></p>
    <p>
      <a href="<%= post_path(post.id) %>" class="btn btn-primary">Lire la suite</a>
      <a href="<%= edit_post_path(post.id) %>" class="btn btn-primary">Editer</a>
      <a href="<%= post_path(post.id) %>" class="btn btn-danger" data-confirm=""Etes vous sur?" data-method="DELETE">Supprimer</a>
    </p>
    <% end %>
